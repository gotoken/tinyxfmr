# AGENTS.md

このリポジトリでは、Transformerを高校数学履修者がボトムアップに理解できる自習教材を開発する。

変更前に必ず以下を読むこと。

- `docs/principles.md` — 教材設計原則
- `docs/curriculum.md` — 全体カリキュラム案
- `docs/development.md` — GUI・コード・数学コラム・レビュー基準
- `docs/data-policy.md` — 外部データの取得・解析・引用・再配布方針

## 最優先原則

新しい概念を「Transformerではそうするから」という理由で導入しない。

原則として、教材の流れを次の順序にする。

1. 現在のモデルを動かす
2. 限界や不便を観察する
3. 欲しい性質を平易な日本語で整理する
4. 解決する操作を導入する
5. GUIで効果を確認する
6. 数式として一般化する
7. 一般的な名称を付ける
8. 実装と対応付ける

数式には目的・必要性・利点を説明する。必要な数学はコラムで正確に補い、比喩でブラックボックス化しない。

## 実装時の注意

- 静的配信を前提とし、核心となる実験はブラウザ内で完結させる。
- 教材コードは高速化や抽象化より、アルゴリズムとの対応の読みやすさを優先する。
- GUIの表示値は可能な限り実際の教材ロジックから取得し、説明専用の架空値を使わない。
- 乱数を使う主要デモは固定seedで再現可能にする。
- 数値安定化など、数式に直接現れない実装上の処理は理由を説明する。
- 新しい用語・数式・ライブラリ依存を追加する場合、読者がその時点で必要性を理解できるか確認する。
- 外部データを使う場合は、公開されていることと再配布可能であることを区別し、出典・ライセンス・変換手順を記録する。
- 各回・各セクションの最後で、到達点と次に残った問題が明確になるようにする。

## 変更時の確認

教材内容を変更した場合は、局所的な正しさだけでなく前後のストーリーが自然か確認する。特に `embedding`, `softmax`, `loss`, `gradient`, `Q/K/V`, `attention`, `residual`, `LayerNorm` などが天下りに登場しないよう注意する。

表示レイアウト、サイト全体のナビゲーション、ビジュアルデザインは別途設計するため、教材内容の初期実装では過度に固定しない。
